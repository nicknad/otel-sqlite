# Local act environment defaults, loaded via --env-file in .actrc.
# Keep this committed with safe values; put local overrides in your shell or
# in a personal copy of .act.env (see .act.env.example).
RUST_LOG=info
CARGO_TERM_COLOR=always