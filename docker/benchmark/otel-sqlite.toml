# Benchmark sink overlay: the harness connects to the server container over
# the compose network (http://otel-sqlite:4317). Since H1 the built-in
# default listen_address is loopback-only; a non-loopback bind is an explicit
# opt-in, as required for a container-network peer.
listen_address = "0.0.0.0:4317"